const { textCyan } = require('../../../cli-shared-utils/chalk');
const { cachePath } = require('../../../cli-shared-utils/cache');
const chalk = require('chalk');

console.log(textCyan(cachePath));

console.log(chalk.gray('asdfasdfasdf'))
